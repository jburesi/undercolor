# Assets Directory

The `assets/` directory contains website resources that require build tool processing.

Common file types include:

- Stylesheets (CSS, SASS, etc.)
- Fonts
- Images (that aren't served from `public/`)

> **Note:** For static assets served directly from the server, use the `public/` directory instead.
